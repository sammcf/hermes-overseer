# Distrobox assemble config for hermes-overseer.
# Usage: distrobox assemble create --file distrobox/overseer.ini [--replace]
[hermes-overseer]
image=archlinux:latest
init=false
nvidia=false
pull=true
start_now=false
additional_packages=python python-pip openssh rsync
